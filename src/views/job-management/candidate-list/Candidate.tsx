import { Box } from '@mui/material'
import { IUser } from 'src/contract/models/user'

const Candidate = ({ candidates }: { candidates: IUser[] }) => {
  return <Box>Candidate List</Box>
}

export default Candidate
